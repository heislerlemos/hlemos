
<% @order_items.each do |item|%>

<h3><%= item.product.titulo  %></h3>
<h5><%= item.product.preco %> </h5>

 
<%=  form_for item, remote: true  do |f| %>


<%= f.hidden_field :product_id, :value => item.product.id %>
<%= f.number_field  :quantity, :value => item.quantity.to_i %>
<%= f.submit "Atualizar" , class: "button" %>

<%= link_to "Remover Item", order_item_path(item), method: :delete, remote: true , class: "button" %>

<% end %>

<h2> Subtotal: <%= item.total %></h2>
<% end %>


<h3> O valor do teu saldo corrente é   <%= current_order.subtotal %> Kz </h3>
